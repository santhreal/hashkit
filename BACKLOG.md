# BACKLOG - hashkit
src/entropy.rs:35 | perf | low | Shannon entropy calculates float division and log2 inside the loop up to 256 times, which can be mathematically optimized to a single division and log2. | FIXED (Law-7): switched to the count-based closed form H = log2(n) - (1/n)Σ c_i log2(c_i), hoisting log2(n) and the per-byte division out of the 256-way loop (one division at the end instead of up to 256). Clamped tiny negative fp residue with .max(0.0). Proving test count_form_matches_naive_probability_form: new form == the naive -Σ p log2 p reference within 1e-9 across 5 inputs (english text, repeated bytes, uniform, skewed, secret-shaped). hashkit lib 60 green. | status=done
tests/property/entropy.rs:123 | bug | medium | PRE-EXISTING RED proptest (surfaced while adding EntropyCounter; NOT caused by that refactor - the closed-form math was kept byte-identical): constant_input_is_zero(byte, len 1..4096) asserts shannon_entropy(vec![byte;len]) == 0.0 EXACTLY, but the closed-form H = log2(n) - n*log2(n)/n leaves a tiny fp residue that can be POSITIVE (e.g. byte=0 len=2861 -> 1.78e-15); `.max(0.0)` only clears NEGATIVE residue, so the assert failed for those inputs. proptest SourceParallel persistence is broken here ("failed to find lib.rs"), so each run explores fresh seeds and intermittently hits a failing len. | FIXED IN CODE (not the test - the test's contract is mathematically correct): a single-distinct-symbol distribution has entropy EXACTLY 0 (matches the naive -Σ p log2 p where p=1 -> -1*log2(1)==0). entropy_from_histogram now counts distinct non-zero lanes and returns 0.0 directly when distinct<=1, before the residue-prone subtraction. Aligns the optimized closed-form with the exact answer for constant input. Full hashkit suite now green incl property_entropy (was RED). | status=done
src/sha256_hash.rs:61 | perf | low | `integrity` performs a double allocation: first when base64 encoding the digest, and second when formatting it with the `sha256-` prefix. | FIXED: now pre-allocates a String::with_capacity(51) ("sha256-"=7 + base64(32 bytes)=44), push_str the prefix, and STANDARD.encode_string directly into it - one allocation instead of two. Existing integrity/parse round-trip tests stay green (lib 60). | status=done
src/hex.rs:86 | perf | low | `decode` performs manual indexing `bytes[i]` and `bytes[i + 1]` in a while loop, which incurs bounds checking overhead on every iteration. | FIXED: rewrote the loop over `bytes.chunks_exact(2).enumerate()`; pair[0]/pair[1] index a known-len-2 slice so the bounds checks are elided. Absolute byte offset (chunk_idx*2) preserved for accurate InvalidCharacter error indices. Byte-iteration + odd-length guard (the multibyte silent-accept fix) unchanged. lib 60 green. | status=done
src/wyhash.rs:29 | perf | low | `wymum` uses `u64::try_from` with `unwrap_or_else` for 128-bit to 64-bit conversion, adding unnecessary complexity and potential branch overhead. | FIXED: `low = product as u64; high = (product >> 64) as u64;` - direct truncation is the actual operation (extract low/high halves), no fallible path + unreachable panic. Added `#[allow(clippy::cast_possible_truncation)]` on the fn (crate has #![deny(clippy::pedantic)], which is why the try_from was there). Behavior-identical; existing wyhash vectors green (lib 60). | status=done
src/hex.rs:14 | bug | low | `DecodeError::InvalidCharacter` stores a `char` obtained by casting a raw byte (`c as char`), which reports incorrect and misleading character names in error messages. | FIXED: variant field is now `byte: u8` (decode iterates bytes, so the offender is a u8); Display renders `invalid byte 0x{byte:02X} at index {index}`. A UTF-8 continuation byte like 0xC3 no longer misrenders as 'Ã'. No external Rust caller matches this variant (grep libs: only hashkit), so the field-type change is contained. Proving test invalid_byte_error_reports_raw_byte_not_misleading_char (decode("é") -> byte 0xC3 at index 0, exact Display). | status=done
tests/unit/audit_content_addressing.rs:88 | test-gap | medium | `wyhash_length_counter_u32_boundary_simulation` does not invoke the actual wyhash library functions, merely asserting basic Rust integer properties. | FIXED (Law 6, removed): the test + wyhash_max_length_handling (:107) asserted only integer arithmetic (u32::MAX cast, usize>=u32) and never called wyhash. Removed both; the feasibly-testable length behavior is already covered by wyhash_length_affects_hash_at_boundaries + wyhash_large_input_no_counter_overflow (the real 4GB boundary is untestable without a 4GB allocation). Replaced with an explanatory NOTE comment. | status=done
tests/unit/audit_content_addressing.rs:107 | test-gap | medium | `wyhash_max_length_handling` is a dummy test that only asserts `usize::MAX as u64 >= u32::MAX as u64`, failing to verify the wyhash code itself. | FIXED: removed together with :88 (same Law-6 tautology); coverage note added. | status=done
tests/unit/audit_content_addressing.rs:230 | stub | low | `blake3_recommended_for_content_hashing` is a documentation-only test that only asserts `std::mem::size_of::<u64>() == 8`. | FIXED: removed (asserted a Rust guarantee, not wyhash behavior, Law 6); its BLAKE3-vs-wyhash guidance is documentation and left as a NOTE comment pointing to module/README docs. | status=done
src/blake3_hash.rs:96 | dedup | low | `blake3_hash::secure_compare` duplicates the general `hashkit::secure_compare` function, adding API redundancy. | FIXED (ONE-PLACE, no API break): rather than deprecate (which would churn the doctest + the p18 proptest + internal tests into deprecation warnings for a low-value typed convenience fn), made blake3's `secure_compare(&[u8;32],&[u8;32])` a 1-line forward to `crate::secure_compare(a.as_slice(), b.as_slice())`. The constant_time_eq call now lives in exactly ONE place; blake3's remains only a type-narrowing entry point. lib 60 green, 0 warnings. | status=done
Cargo.toml:90 | dedup | medium | Property test files (blake3.rs, invariants.rs, large_fuzz.rs) are compiled and executed twice: once as standalone tests, and once as submodules of `property_property` (property.rs). | FIXED: removed the 3 standalone [[test]] targets (property_blake3/invariants/large_fuzz); kept the property_property aggregator that #[path]-includes them, so each file compiles+runs once. Added a Cargo.toml comment documenting the aggregator-vs-standalone rule. property_entropy kept (entropy.rs is NOT aggregated). | status=done
Cargo.toml:82 | dedup | low | `integration_bloom` is compiled and executed twice: once as a standalone test target, and once as a submodule of `integration_integration`. | FIXED: removed the standalone integration_bloom target; bloom.rs now compiles+runs once via the integration_integration aggregator (which #[path]-includes it). | status=done
tests/adversarial/adversarial_hash.rs:1 | dedup | medium | `tests/adversarial/adversarial_hash.rs` is an almost 100% identical copy of `tests/adversarial/adversarial.rs`, and both are registered as separate tests in Cargo.toml, wasting test compilation and execution time. | FIXED: verified via sorted diff that adversarial_hash.rs is a strict SUBSET of adversarial.rs (identical test fns; adversarial.rs additionally #[path]-includes exhaust+kats). Deleted adversarial_hash.rs + its adversarial_adversarial_hash target - zero coverage lost. SAME-CLASS BONUS (found while here): adversarial_exhaust + adversarial_kats standalone targets were ALSO double-compiled (exhaust.rs/kats.rs are #[path]-included by adversarial.rs), so removed those two standalone targets too. All targets compile; aggregators run green (property_property 18, integration_integration 2). | status=done
tests/unit/audit_content_addressing.rs:88 | test-gap | medium | `wyhash_length_counter_u32_boundary_simulation` does not invoke the actual wyhash library functions, merely asserting basic Rust integer properties. | RECONCILED: STALE DUPLICATE of the :88 row above (already FIXED - the tautology test was removed, Law 6, replaced with a coverage NOTE). Dedup-on-file:line. | status=done
tests/unit/audit_content_addressing.rs:107 | test-gap | medium | `wyhash_max_length_handling` is a dummy test that only asserts `usize::MAX as u64 >= u32::MAX as u64`, failing to verify the wyhash code itself. | RECONCILED: STALE DUPLICATE of the :107 row above (already removed together with :88). Dedup-on-file:line. | status=done
tests/unit/audit_content_addressing.rs:230 | stub | low | `blake3_recommended_for_content_hashing` is a documentation-only test that only asserts `std::mem::size_of::<u64>() == 8`. | RECONCILED: STALE DUPLICATE of the :230 row above (already removed; BLAKE3-vs-wyhash advice moved to a NOTE comment). Dedup-on-file:line. | status=done
src/entropy.rs:18 | capability | medium | Shannon entropy can only be computed on a full byte slice loaded in memory. There is no streaming/incremental API for computing entropy on large files or streams. | FIXED: added a public `EntropyCounter` accumulator - `new()` / `update(&[u8])` (fold a chunk) / `entropy()` / `bucket()` / `len()` / `is_empty()` - holding only a fixed 256-lane u64 histogram (2 KiB) + a running total, so entropy over an arbitrarily large stream (a file read in chunks, a socket) is O(1) memory. Refactored the entropy math into a ONE-PLACE `entropy_from_histogram(&[u64;256], total)` and the quantization into `bucket_from_entropy(f64)`, both shared by the slice API (shannon_entropy/entropy_bucket) and the streaming counter, so they compute bit-identically (no divergent second implementation). Proving tests: streaming_matches_slice_across_inputs_and_chunkings (4 inputs x chunk sizes {1,3,7,16} each assert `counter.entropy() == shannon_entropy(whole)` within 1e-9 AND bucket equality - proves chunk-boundary independence + slice equivalence), uniform_stream_reaches_max_entropy (256 distinct bytes one-at-a-time -> 8.0 bits, bucket 255), repeated_byte_stream_has_zero_entropy, empty_counter_is_zero_and_empty. Plus a doctest showing split-then-recombine equals the whole. Existing slice tests unchanged and green (10 entropy tests total). | status=done
src/lib.rs:96 | capability | low | The crate lacks a helper function to directly generate $k$ bloom filter probe indices, forcing users to manually implement the double-hashing step. | FIXED: added `pub fn bloom_probes(h1, h2, k, num_bits) -> impl Iterator<Item=usize>` implementing canonical Kirsch-Mitzenmacher double hashing (g_i = hash_to_index(h1 + i*h2, num_bits) for i in 0..k; wrapping_add/mul so large i never overflows). Lazy iterator (caller decides whether to allocate), num_bits==0 -> all-zero (matches hash_to_index). Replaces the hand-rolled `h1 + i*h2` stepping consumers did inline (e.g. tests/integration/bloom.rs). Proving tests (lib): bloom_probes_follow_kirsch_mitzenmacher_formula (exact values vs manual (h1+i*h2)%1000 over a NON-power-of-two width to exercise the modulo path; asserts count==5), bloom_probes_first_is_h1_reduced_and_all_in_bounds (probe[0]==hash_to_index(h1) + all <num_bits across 5 widths incl non-pow2), bloom_probes_zero_k_yields_no_indices. + a doctest. Full hashkit: 67 lib + 22 doc green. | status=done